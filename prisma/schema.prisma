

generator client {
  provider = "prisma-client"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
 
}
model Tasks{
id   String @id @default(uuid())
  task String
  done Boolean
}